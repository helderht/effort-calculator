export default function valitadeMonth(nro) {
  if (nro < 1 || nro > 30) return true
}
export function validateDay(nro) {
  if (nro < 1 || nro > 23) return true
}
export function validateTeam(nro) {
  if (nro < 1) return true
}
